# Debian base image

This image is used as base for other images created from me.

It extends the Debian image by curl and sets the timezone to Europe/Berlin.

## Development

To build the image locally run:
```bash
./docker-build.sh
```